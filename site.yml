---
# This playbook deploys the whole application stack in this site for now.

- name: common settings for all sites
  hosts: all

  roles:
    - { role: apt,         become: true }
    - { role: common,      become: true }
    - { role: programming, become: true }
